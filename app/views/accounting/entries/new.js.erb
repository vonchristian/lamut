$("#entryModal").html("<%= escape_javascript(render 'new') %>")
$('#entryModal').modal({backdrop: 'static', keyboard: false, show: true})
$('.datepicker').datepicker({
	format: "yyyy/mm/dd/",
	orientation: "auto bottom",
	todayHighlight: 'true',
	autoclose: 'true'
	});
$("#accounting_entry_debit_amounts_attributes_0_account").select2({
  theme: "bootstrap",
  placeholder: 'Select Account',
  width: '100%',
  dropdownAutoWidth : true
});
$("#accounting_entry_credit_amounts_attributes_0_account").select2({
  theme: "bootstrap",
  placeholder: 'Select Account',
  width: '100%',
  dropdownAutoWidth : true
});
